name: shared_config
description: Shared configuration for OnlyGigz apps
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  url_launcher: ^6.3.1
  firebase_core: ^3.13.0
  firebase_remote_config: ^5.4.6
